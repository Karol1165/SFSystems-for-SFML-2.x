SFGE is "Simple and fast game framework" based on SFML 2.5.1 (compatible with later versions)

Framework has many game elements (scenes, buttons etc.).

Using:
You must declare void (void) functions that are doing one time actions (scene initialization, button action etc.).
You can draw scenes and single objects.
Use update methods in every frame.
